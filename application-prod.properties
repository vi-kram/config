# ===================================
# SHARED PROD PROFILE
# ===================================

# Logging — less verbose in prod
logging.level.root=WARN
logging.level.com.capg=INFO
logging.level.org.hibernate.SQL=WARN

# Hide SQL in prod
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
